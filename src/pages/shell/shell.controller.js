angular
  .module('macChat')
  .controller('shellCtrl', function(socketService) {
    // socketService.connect();
    // socketService.on('connect', function(event, data) {
    //   console.log('yoo connected');
    // });
  });